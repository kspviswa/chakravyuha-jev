{
  "name": "chakravyuha",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Chakravyuha — a polar ring-maze where every move is a Jev decision. No pathfinding in the game loop: the maze is serialised to text, sent to Jev, and the move Jev picks is applied verbatim, then checked by a referee.",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "verify": "node scripts/verify-browser.mjs",
    "verify:deployed": "node scripts/verify-deployed.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}